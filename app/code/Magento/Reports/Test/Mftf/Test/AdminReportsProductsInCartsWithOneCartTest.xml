<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminReportsProductsInCartsWithOneCartTest">
        <annotations>
            <features value="Reports"/>
            <stories value="Products in Cart Reports"/>
            <title value="Products in Cart Reports - Variation with the product in one cart"/>
            <description value="Verify that 'Products in Cart' Report show correct information when product was added to one cart"/>
            <severity value="AVERAGE"/>
            <group value="reports"/>
            <group value="mtf_migrated"/>
        </annotations>
        <before>
            <createData entity="_defaultCategory" stepKey="createCategory"/>
            <createData entity="_defaultProduct" stepKey="createProduct">
                <requiredEntity createDataKey="createCategory"/>
            </createData>
            <createData entity="Simple_US_Customer" stepKey="createCustomer"/>
        </before>
        <after>
            <deleteData createDataKey="createCustomer" stepKey="deleteCustomer"/>
            <deleteData createDataKey="createProduct" stepKey="deleteProduct"/>
            <deleteData createDataKey="createCategory" stepKey="deleteCategory"/>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logoutAdmin"/>
        </after>
        <!-- Login as customer -->
        <actionGroup ref="LoginToStorefrontActionGroup" stepKey="logInFromCustomer">
            <argument name="Customer" value="$$createCustomer$$"/>
        </actionGroup>
        <!-- Add product to cart -->
        <actionGroup ref="AddSimpleProductToCartActionGroup" stepKey="addProductToCart">
            <argument name="product" value="$$createProduct$$"/>
        </actionGroup>
        <!-- Logout customer -->
        <actionGroup ref="StorefrontCustomerLogoutActionGroup" stepKey="customerLogoutStorefront"/>
        <!-- Login to admin panel -->
        <actionGroup ref="AdminLoginActionGroup" stepKey="loginToAdminPanel"/>
        <!-- Go to Products in Cart Report -->
        <actionGroup ref="AdminNavigateMenuActionGroup" stepKey="navigateToReportsProductsInCartPage">
            <argument name="menuUiId" value="{{AdminMenuReports.dataUiId}}"/>
            <argument name="submenuUiId" value="{{AdminMenuReportsMarketingProductsInCarts.dataUiId}}"/>
        </actionGroup>
        <!-- Assert that element with product name exists on grid -->
        <actionGroup ref="AdminAssertProductPresentOnProductsInCartsReportActionGroup" stepKey="assertProductIsPresent">
            <argument name="productName" value="$$createProduct.name$$"/>
        </actionGroup>
        <!-- Grab the search result values -->
        <grabTextFrom selector="{{AdminProductsInCartsReportMainSection.productsInCartsReportsGridCell($$createProduct.name$$, 'Carts')}}" stepKey="grabCartsFromGrid"/>
        <!-- Assert that Carts number in Grid are correct -->
        <assertEquals expected="1" expectedType="string" actual="$grabCartsFromGrid" stepKey="assertCartsNumCorrect"/>
    </test>
</tests>
